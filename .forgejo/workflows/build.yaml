on:
  push:
    branches:
      - master
jobs:
  build:
    runs-on: docker
    steps:
      - uses: actions/checkout@master
      - uses: actions/setup-node@master
      - run: npm i
      - run: npm run build
